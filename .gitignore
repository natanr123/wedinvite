# Dependencies & build output
node_modules/
dist/
.next/

# Environment — every .env* variant stays out of git except the committed templates
.env*
!.env.example
!.env.prod.example

# Temp / generated artifacts — keep all scratch files under tmp/
tmp/
.playwright-mcp/
test-results/
playwright-report/
*.log

# Editor / OS
.DS_Store
.idea/
.venv/

# Platform CLI local state (may contain project ids / linkage)
.vercel/
supabase/.temp/
supabase/.branches/
