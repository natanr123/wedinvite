# Local development — copy to .env. Matches docker-compose.yml at the repo root.
DATABASE_URL=postgres://wedinvite:wedinvite@127.0.0.1:54329/wedinvite
# Schema sync from entities — local dev convenience only (prod uses migrations).
DATABASE_SYNC=true
PORT=3001
